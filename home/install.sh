#!/bin/bash

# add any commands there that should run when the container is initialized
# ex.
# sudo apt-get install -y ...

# do not remove the line below: this indicates the install has completed
sudo touch /usr/local/install_complete